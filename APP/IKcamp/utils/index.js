import * as Mock from './mock';
const util = {
    request(opt) {
        let options = Object.assign(
            {},
            DEFAULT_OPTION,
            opt
        )
        const {mock} = options;
        return new Promise((resolve,reject) => {
            let url = jh
        })
    }
}
export default util