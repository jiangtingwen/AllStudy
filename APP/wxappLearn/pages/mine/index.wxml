<!--pages/mine/index.wxml-->
<view>
  <!-- <ul  wx:for="{{people}}" wx:key="{{index}}">
    <li>{{item.name}}</li>
  </ul> -->
    <ul>
    <li wx:for="{{people}}" wx:key="{{index}}">{{item.name}}</li>
  </ul>
</view>
